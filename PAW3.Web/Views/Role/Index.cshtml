@model IEnumerable<RoleViewModel>

@{
    ViewData["Title"] = "Roles";
}

<div class="content-card">
    <div class="page-header">
        <h1><i class="fas fa-user-shield text-primary-custom me-2"></i>Roles</h1>
        <a asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus me-2"></i>Create New
        </a>
    </div>

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger" role="alert">
            <i class="fas fa-exclamation-circle me-2"></i>@ViewBag.Error
        </div>
    }

    @if (Model != null && Model.Any())
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.RoleId)</th>
                        <th>@Html.DisplayNameFor(model => model.RoleName)</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td><strong>@Html.DisplayFor(modelItem => item.RoleId)</strong></td>
                            <td>@Html.DisplayFor(modelItem => item.RoleName)</td>
                            <td class="text-end">
                                <div class="action-buttons">
                                    <a asp-controller="Role" asp-action="Details" asp-route-id="@item.RoleId" class="btn btn-info btn-sm">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <a asp-controller="Role" asp-action="Edit" asp-route-id="@item.RoleId" class="btn btn-warning btn-sm">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a asp-controller="Role" asp-action="Delete" asp-route-id="@item.RoleId" class="btn btn-danger btn-sm">
                                        <i class="fas fa-trash"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="empty-state">
            <div class="empty-state-icon">
                <i class="fas fa-user-shield"></i>
            </div>
            <h3>No Roles Found</h3>
            <p>Get started by creating your first role.</p>
            <a asp-action="Create" class="btn btn-primary mt-3">
                <i class="fas fa-plus me-2"></i>Create Role
            </a>
        </div>
    }
</div>
